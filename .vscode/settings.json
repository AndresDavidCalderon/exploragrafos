// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "**/*.uid": true,
        "**/*.aseprite": true,
        "**/*.import": true,
        "**/*.TMP": true,
        "**/*.tmp": true
    }
}